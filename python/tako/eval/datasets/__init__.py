"""Built-in eval datasets.

`synthetic.jsonl` ships in-tree (loaded by `tako.eval.load_synthetic`).
External datasets (SWE-Bench-Lite, GPQA-Diamond) are fetched on demand
from Hugging Face via `external.py`, which requires
``pip install tako[eval]``.
"""
